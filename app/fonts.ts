import { Inspiration, Varta } from 'next/font/google';

export const fontInspiration = Inspiration({
	weight: '400',
	subsets: ['latin'],
});

export const fontVarta = Varta({
	weight: ['400', '700'],
	subsets: ['latin'],
});
